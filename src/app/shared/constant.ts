export enum DateTypeFilter {
    TODAY  = 'today',
    LAST7DAYS = 'last7days',
    LAST30DAYS = 'last30days'
}

export enum APP_PATH {
    FORGOT_PASSWORD = 'forgot-password',
    LOGIN = 'login',
    USERS = 'users',
    ADD_USER = 'user-add'
}